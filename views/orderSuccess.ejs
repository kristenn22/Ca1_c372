<h2>Order Successful!</h2>

<p>Order ID: <%= order.id %></p>
<p>Total Paid: $<%= order.total.toFixed(2) %></p>
<p>Shipping to: <%= order.address %></p>

<h4 class="mt-3">Items:</h4>
<ul>
  <% items.forEach(i => { %>
    <li><%= i.productName %> (x<%= i.quantity %>) - $<%= i.price.toFixed(2) %></li>
  <% }) %>
</ul>

<a href="/order-history" class="btn btn-primary mt-3">View Order History</a>
