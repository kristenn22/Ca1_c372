<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Transaction Failed</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        .fail-container {
            background: white;
            padding: 50px 40px;
            border-radius: 15px;
            box-shadow: 0 10px 40px rgba(0,0,0,0.2);
            text-align: center;
            max-width: 500px;
            animation: slideIn 0.5s ease-out;
        }
        @keyframes slideIn {
            from {
                opacity: 0;
                transform: translateY(-30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        .fail-icon {
            width: 80px;
            height: 80px;
            background-color: #dc3545;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 30px;
            font-size: 50px;
            color: white;
        }
        .fail-container h1 {
            color: #dc3545;
            font-weight: bold;
            margin-bottom: 15px;
            font-size: 32px;
        }
        .fail-container p {
            color: #666;
            margin-bottom: 30px;
            font-size: 16px;
        }
        .fail-container a {
            display: inline-block;
            margin: 10px;
        }
    </style>
</head>
<body>
    <div class="fail-container">
        <div class="fail-icon">✕</div>
        <h1><%= message %></h1>
        <p>We encountered an issue processing your payment. Please try again or contact support if the problem persists.</p>
        <a href="/cart/checkout" class="btn btn-danger">Try Again</a>
        <a href="/shopping" class="btn btn-outline-secondary">Back to Shopping</a>
    </div>
</body>
</html>
