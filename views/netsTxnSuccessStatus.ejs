<!DOCTYPE html>
<html>
<head>
    <title>Transaction Successful</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <%- include('partials/navbar') %>
    <div class="container mt-5 text-center">
        <h1 class="text-success"><%= message %></h1>
        <p class="lead">Thank you for your payment!</p>
        <img height="auto" width="20%" src="../images/greenTick.png" class="mb-4" /><br />
        <% if (orderId) { %>
            <div class="mt-4">
                <a href="/invoice/<%= orderId %>" class="btn btn-primary me-2">View Invoice</a>
                <a href="/shopping" class="btn btn-outline-secondary">Continue Shopping</a>
            </div>
        <% } %>
    </div>
</body>
</html>
