<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Transactions - Admin Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        :root {
            --primary-color: #007bff;
            --success-color: #28a745;
            --warning-color: #ffc107;
            --danger-color: #dc3545;
            --light-bg: #f8f9fa;
            --card-shadow: 0 2px 8px rgba(0,0,0,0.1);
        }

        body {
            background-color: var(--light-bg);
        }

        .transactions-header {
            background: linear-gradient(135deg, var(--primary-color) 0%, #0056b3 100%);
            color: white;
            padding: 30px 0;
            margin-bottom: 30px;
        }

        .filter-buttons {
            display: flex;
            gap: 10px;
            margin-bottom: 20px;
            flex-wrap: wrap;
        }

        .filter-btn {
            padding: 10px 20px;
            border: 2px solid #ddd;
            background: white;
            border-radius: 8px;
            cursor: pointer;
            font-weight: 600;
            transition: all 0.3s;
            text-decoration: none;
            color: #333;
        }

        .filter-btn:hover {
            border-color: var(--primary-color);
            color: var(--primary-color);
        }

        .filter-btn.active {
            background: var(--primary-color);
            color: white;
            border-color: var(--primary-color);
        }

        .summary-card {
            background: white;
            border-radius: 12px;
            padding: 20px;
            margin-bottom: 25px;
            box-shadow: var(--card-shadow);
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .summary-item {
            text-align: center;
        }

        .summary-label {
            color: #6c757d;
            font-size: 0.9rem;
            text-transform: uppercase;
            margin-bottom: 8px;
        }

        .summary-value {
            font-size: 2rem;
            font-weight: 700;
            color: var(--primary-color);
        }

        .transactions-table {
            background: white;
            border-radius: 12px;
            box-shadow: var(--card-shadow);
            overflow: hidden;
        }

        .transactions-table table {
            margin-bottom: 0;
        }

        .transactions-table thead {
            background-color: #f8f9fa;
            border-bottom: 2px solid #dee2e6;
        }

        .transactions-table th {
            font-weight: 700;
            color: #333;
            padding: 15px;
            vertical-align: middle;
        }

        .transactions-table td {
            padding: 15px;
            vertical-align: middle;
            border-bottom: 1px solid #dee2e6;
        }

        .transactions-table tbody tr:hover {
            background-color: #f8f9fa;
        }

        .badge-success {
            background-color: var(--success-color);
            color: white;
            padding: 6px 12px;
            border-radius: 6px;
            font-size: 0.85rem;
        }

        .badge-warning {
            background-color: var(--warning-color);
            color: #333;
            padding: 6px 12px;
            border-radius: 6px;
            font-size: 0.85rem;
        }

        .amount {
            font-weight: 700;
            color: var(--success-color);
        }

        .no-transactions {
            text-align: center;
            padding: 40px 20px;
            color: #6c757d;
        }

        .no-transactions svg {
            width: 80px;
            height: 80px;
            opacity: 0.3;
            margin-bottom: 20px;
        }

        .back-link {
            display: inline-block;
            margin-bottom: 20px;
            color: var(--primary-color);
            text-decoration: none;
            font-weight: 600;
        }

        .back-link:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>

<%- include('partials/navbar') %>

<!-- Header -->
<div class="transactions-header">
    <div class="container">
        <a href="/adminDashboard" class="back-link">← Back to Dashboard</a>
        <h1>📊 Transactions</h1>
        <p>View and filter all transactions by time period</p>
    </div>
</div>

<!-- Main Container -->
<div class="container">

    <!-- Filter Buttons -->
    <div class="filter-buttons">
        <a href="/admin/transactions?filter=today" class="filter-btn <%= currentFilter === 'today' ? 'active' : '' %>">
            📅 Today
        </a>
        <a href="/admin/transactions?filter=week" class="filter-btn <%= currentFilter === 'week' ? 'active' : '' %>">
            📆 This Week
        </a>
        <a href="/admin/transactions?filter=month" class="filter-btn <%= currentFilter === 'month' ? 'active' : '' %>">
            📋 This Month
        </a>
        <a href="/admin/transactions?filter=year" class="filter-btn <%= currentFilter === 'year' ? 'active' : '' %>">
            📅 This Year
        </a>
        <a href="/admin/transactions?filter=lastYear" class="filter-btn <%= currentFilter === 'lastYear' ? 'active' : '' %>">
            📅 Last Year
        </a>
    </div>

    <!-- Summary Cards -->
    <div class="summary-card">
        <div class="summary-item">
            <div class="summary-label">Total Transactions</div>
            <div class="summary-value"><%= totalTransactions %></div>
        </div>
        <div class="summary-item">
            <div class="summary-label">Total Earnings</div>
            <div class="summary-value" style="color: var(--success-color);">$<%= totalEarnings %></div>
        </div>
        <div class="summary-item">
            <div class="summary-label">Average Order Value</div>
            <div class="summary-value">$<%= totalTransactions > 0 ? (totalEarnings / totalTransactions).toFixed(2) : '0.00' %></div>
        </div>
    </div>

    <!-- Transactions Table -->
    <% if (transactions && transactions.length > 0) { %>
        <div class="transactions-table">
            <table class="table">
                <thead>
                    <tr>
                        <th>Order ID</th>
                        <th>Customer</th>
                        <th>Email</th>
                        <th>Payment Method</th>
                        <th>Subtotal</th>
                        <th>Shipping</th>
                        <th>Total</th>
                        <th>Date & Time</th>
                    </tr>
                </thead>
                <tbody>
                    <% transactions.forEach(transaction => { %>
                        <tr>
                            <td>
                                <strong>#<%= transaction.orderId %></strong>
                            </td>
                            <td><%= transaction.username || 'Guest' %></td>
                            <td><%= transaction.email || 'N/A' %></td>
                            <td>
                                <% if (transaction.paymentMethod === 'PayPal') { %>
                                    <span class="badge-success">PayPal</span>
                                <% } else if (transaction.paymentMethod === 'NETS QR') { %>
                                    <span class="badge-warning">NETS QR</span>
                                <% } else { %>
                                    <span class="badge-warning"><%= transaction.paymentMethod %></span>
                                <% } %>
                            </td>
                            <td>$<%= parseFloat(transaction.subtotal).toFixed(2) %></td>
                            <td>$<%= parseFloat(transaction.shipping).toFixed(2) %></td>
                            <td class="amount">$<%= parseFloat(transaction.total).toFixed(2) %></td>
                            <td><%= transaction.formattedDate %></td>
                        </tr>
                    <% }); %>
                </tbody>
            </table>
        </div>
    <% } else { %>
        <div class="no-transactions">
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
            </svg>
            <h4>No Transactions Found</h4>
            <p>There are no transactions for the selected period.</p>
        </div>
    <% } %>

    <!-- Back to Dashboard Button -->
    <div style="text-align: center; margin-top: 30px; padding: 20px;">
        <a href="/adminDashboard" class="btn btn-primary btn-lg">← Back to Dashboard</a>
    </div>

</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>
